# bblocks-xslt2-transform-plugin

Adds a saxonche xslt transformer to the OGC Building Blocks postprocessor

uses SaxonChe as processor instead of the lxml version that the standard xslt transformer uses.